<%= tag.div id: dom_id(result), class: "flex gap-4" do %>
  <div class="w-20"><%= result.success? ? "Success" : "Fail" %></div>
  <div class="text-gray-700"><%= time_ago_in_words(result.created_at).upcase_first  %> ago</div>
<% end %>
